# Mengineers Robot
Run:
```
sudo docker build --platform linux/arm64 -t codrone-test . && sudo docker run -it --rm   --device=/dev/ttyACM0:/dev/ttyACM0   --privileged   codrone-test

```
in:
```
~/Mengineers_Robot/codrone_test/codrone_test
```
